spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/skillsync
  profiles:
    active: ${ACTIVE_PROFILE:dev}
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

file:
  upload-dir: uploads

github:
  token: ${GITHUB_TOKEN:}

server:
  port: ${SERVER_PORT:8081}
  servlet:
    context-path: /

app:
  jwt:
    secret: ${JWT_SECRET:KiSoya8tyYYbPnoLGc/ufz+0wKGhc57is8lZkbJEp2pdofwY/t50bazj19AUlIFuciEOho26baR5EsqVI/Rlvg==}
    expiration: ${JWT_EXPIRATION:86400000}
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

logging:
  level:
    com.skillsync: ${LOG_LEVEL_COM_SKILLSYNC:DEBUG}
